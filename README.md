# adjacency_list
PHP class for managing hierarchical tree structures in MySQL 8+ / MariaDB using the adjacency list model with recursive CTEs. Supports insert, delete, move, reorder and rename operations. Auto-creates tables, validates schema, prevents orphaned nodes and circular moves.
